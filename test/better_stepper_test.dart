import 'package:flutter_test/flutter_test.dart';

import 'package:better_stepper/better_stepper.dart';

void main() {
  test('adds one to input values', () {
    final stepper = BetterStepper(steps: [],);
  });
}
